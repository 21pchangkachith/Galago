import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class DescriptionBox here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DescriptionBox extends Display
{
    /**
     * Act - do whatever the DescriptionBox wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    { 
    }    
    public DescriptionBox()
    {
        this.setImage(new GreenfootImage(100, 20));
        this.getImage().setColor(Color.WHITE);
    }
}
